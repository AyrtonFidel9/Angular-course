import { HeroesImagePipe } from './heroes-image.pipe';

describe('HeroesImagePipe', () => {
  it('create an instance', () => {
    const pipe = new HeroesImagePipe();
    expect(pipe).toBeTruthy();
  });
});
